version: '3'
services:
  backend:
    build: ./cloud-gaming-backend
    ports:
      - "5000:5000"
  frontend:
    build: ./cloud-gaming-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
